name: CI

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-slim

    steps:
    - name: Update system
      run: sudo apt update

    - name: Install SSH
      run: sudo apt install -y openssh-server

    - name: Enable SSH
      run: |
        sudo systemctl enable ssh
        sudo systemctl start ssh

    - name: Set runner password
      run: |
        echo "runner:password123" | sudo chpasswd
        sudo sed -i 's/^#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
        sudo sed -i 's/^PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
        sudo systemctl restart ssh

    - name: Download ngrok
      run: |
        wget https://bin.equinox.io/c/bNyj1mQVY4c/ngrok-v3-stable-linux-amd64.zip -O ngrok.zip
        unzip ngrok.zip

    - name: Authenticate ngrok
      run: ./ngrok authtoken $NGROK_AUTH_TOKEN
      env:
        NGROK_AUTH_TOKEN: ${{ secrets.NGROK_AUTH_TOKEN }}

    - name: Create ngrok TCP Tunnel (India Region)
      run: ./ngrok tcp --region=in 22
